public class Main {
    public static void main(String[] args) {

        // LOGGER
        // private static final Logger LOG = Logger.getLogger({ENTITY}.class);

        // CONNECTION

        // ATRIBUTOS
        // public static final String URL = "jdbc:h2:~/{NOMBRE};INIT=RUNSCRIPT from 'create_table.sql.sql'";
        // public static final String H2DRIVER = "org.h2.Driver";
        // public static final String USER = "sa";
        // public static final String PASSWORD = "sa";

        // public Connection levantarConexion() {
        //      connection = null;
        //      try {
        //          Class.forName(H2DRIVER);
        //          connection = DriverManager.getConnection(URL, USER, PASSWORD);
        //      } catch (ClassNotFoundException | SQLException e) {
        //          LOG.error(e.getMessage());
        //      }
        // }

    }
}